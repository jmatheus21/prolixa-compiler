This is program Teste.

It presents:
alterable answer Condicao;
alterable number Num;
alterable symbol Tempo;
unalterable symbol Constante;
vector of number (2 x 3) Vetor;
alterable number Numero;

Let us begin:
Num := 10 // 2;
Condicao := yes;

show(Condicao);

just in case that (Condicao) do capture(Num);
as long as (Condicao) do [
	alterable number NovoNumero;
	NovoNumero := 10;
	capture(Num);
	abandon;
]

That would be all.